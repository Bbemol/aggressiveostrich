# Fonts to add manually

Drop your licensed font files here:

- `MADEOuterSansPERSONALUSE-Regular.otf` (used as default font)
- `Motterdam-K74zp.otf` (used for display text)

The CSS is already wired in `styles/design-system.css`.
